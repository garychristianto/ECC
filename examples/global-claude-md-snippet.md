# Global CLAUDE.md snippet — paste into ~/.claude/CLAUDE.md on desktop

> This is a GLOBAL (user-scoped) preference. It belongs in `~/.claude/CLAUDE.md`,
> NOT a project's `CLAUDE.md`. Once added, every Claude Code session on your
> machine — any repo, any folder — follows it.
>
> Copy everything below the line into `~/.claude/CLAUDE.md` (create the file if it
> doesn't exist).

---

## Response style

- Begin every response with an `## Executive summary` section: 1–4 lines giving the
  answer, recommendation, or bottom line up front.
- Follow with a `## Details` section containing the supporting reasoning, steps,
  tables, commands, and caveats — so I can read deeper only if I want to.
- Trivial or one-line answers may be summary-only; don't pad them.
